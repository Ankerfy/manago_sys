`NProgress` 是一个轻量级的顶部进度条库，提供了简洁但灵活的配置选项。通过 `NProgress.configure(options)` 方法可以自定义其行为和外观。

---

## ✅ NProgress 配置参数详解

### 🔧 使用方式

```js
import NProgress from 'nprogress'

NProgress.configure({
  // 配置项写在这里
})
```

---

### 📋 完整配置参数列表及说明

| 参数                  | 类型                  | 默认值               | 作用说明                                                  |
| --------------------- | --------------------- | -------------------- | --------------------------------------------------------- |
| **`minimum`**         | `Number` (0~1)        | `0.08`               | 进度条起始最小百分比（如设为 `0.1` 表示从 10% 开始）      |
| **`easing`**          | `String`              | `'ease'`             | CSS 动画缓动函数，控制进度条推进的速度曲线                |
| **`speed`**           | `Number` (ms)         | `200`                | 动画速度（毫秒），值越小越快                              |
| **`showSpinner`**     | `Boolean`             | `true`               | 是否显示右上角的小旋转图标（spinner）                     |
| **`trickle`**         | `Boolean`             | `true`               | 是否启用“细流”模式：自动缓慢增加进度（模拟未知加载时间）  |
| **`trickleRate`**     | `Number` (0~1)        | `0.02`               | “细流”模式下每次增加的进度比率（如每 500ms 增加 2%）      |
| **`trickleSpeed`**    | `Number` (ms)         | `500`                | “细流”模式下进度增加的时间间隔                            |
| **`parent`**          | `String\|HTMLElement` | `'body'`             | 指定进度条插入的父容器（默认是 `<body>`）                 |
| **`positionUsing`**   | `String`              | `'right'`            | 控制进度条定位方式：`'right'` 或 `'left'`（影响动画方向） |
| **`barSelector`**     | `String`              | `'[role="bar"]'`     | 用于选择进度条元素的选择器（高级用法）                    |
| **`spinnerSelector`** | `String`              | `'[role="spinner"]'` | 用于选择 spinner 元素的选择器                             |
| **`template`**        | `String`              | 内置 HTML            | 自定义进度条 DOM 结构模板                                 |

---

### 🧩 参数详细解释

#### 1. `minimum: Number`

- 控制进度条从多少开始。
- 例如设置为 `0.1`，则每次调用 `NProgress.start()` 时从 10% 开始。
- 避免让用户感觉“卡在 0”。

#### 2. `easing: String`

- CSS 的 `transition-timing-function`。
- 可选值：`'linear'`, `'ease'`, `'ease-in'`, `'ease-out'`, `'ease-in-out'`, `'cubic-bezier(x,x,x,x)'`
- 示例：`easing: 'cubic-bezier(0.4, 0, 0.2, 1)'`（类似 Material Design）

#### 3. `speed: Number`

- 控制动画过渡时间。
- 设置为 `300` 表示动画持续 300 毫秒。
- 太快会显得突兀，太慢会显得卡顿，推荐 `200~500`。

#### 4. `showSpinner: Boolean`

- 是否显示右上角的小圆圈旋转动画。
- 在大多数现代 UI 中建议关闭：`showSpinner: false`

#### 5. `trickle: Boolean`

- 是否开启“细流”模式。
- 当你不知道加载何时结束时，它会自动缓慢增加进度（防止停滞）。
- 推荐保持开启。

#### 6. `trickleRate: Number`

- 每次“细流”增加的进度值。
- 如 `0.02` 表示每次增加 2%。
- 若 `trickleSpeed: 500`，则每 0.5 秒增加 2%。

#### 7. `trickleSpeed: Number`

- “细流”进度增加的间隔时间（毫秒）。
- 与 `trickleRate` 配合使用。

#### 8. `parent: String \| HTMLElement`

- 指定将进度条插入哪个 DOM 容器。
- 默认是 `body`，即全屏顶部。
- 如果你的应用在一个容器内（如 `#app`），可设为 `'#app'`

#### 9. `positionUsing: String`

- 决定使用 `left` 还是 `right` 来控制进度条宽度动画。
- 影响动画方向感，默认 `'right'`（从左向右推）

#### 10. `barSelector` & `spinnerSelector`

- 高级用法：用于匹配自定义模板中的 bar 和 spinner 元素。
- 一般不需要修改。

#### 11. `template: String`

- 自定义进度条的 HTML 结构。
- 默认值：

  ```html
  '
  <div class="bar" role="bar"><div class="peg"></div></div>
  <div class="spinner" role="spinner"><div class="spinner-icon"></div></div>
  '
  ```

- 可用于移除 spinner 或添加自定义类名。

---

### ✅ 推荐配置示例（结合 Element Plus 风格）

```js
// src/main.js 或 router守卫前调用
import NProgress from 'nprogress'
import 'nprogress/nprogress.css'

NProgress.configure({
  minimum: 0.1, // 从 10% 开始
  easing: 'ease', // 缓动效果
  speed: 300, // 动画速度适中
  showSpinner: false, // 关闭右上角小图标
  trickle: true, // 启用细流模式
  trickleRate: 0.02, // 每次增加 2%
  trickleSpeed: 500, // 每 500ms 增加一次
  parent: 'body', // 插入 body
  positionUsing: 'right', // 使用 right 定位
})
```

---

### 💡 小技巧：隐藏 spinner 的另一种方式

即使 `showSpinner: true`，你也可以用 CSS 隐藏：

```css
/* 隐藏 spinner */
#nprogress .spinner {
  display: none !important;
}
```

---

## ✅ 总结

| 场景         | 推荐配置                                                  |
| ------------ | --------------------------------------------------------- |
| 简洁现代风   | `showSpinner: false`                                      |
| 快速响应感   | `speed: 200`, `minimum: 0.15`                             |
| 模拟真实加载 | `trickle: true`, `trickleRate: 0.03`, `trickleSpeed: 400` |
| 自定义容器   | `parent: '#app'`                                          |
| 平滑动画     | `easing: 'cubic-bezier(0.4, 0, 0.2, 1)'`                  |

---

✅ **最佳实践**：  
在项目初始化时（如 `main.js`）统一配置一次 `NProgress.configure()`，然后在路由守卫中调用 `start()` 和 `done()`，即可实现**全局优雅的顶部加载进度条**。

如果你希望我提供一个 **可复用的 progress 插件封装** 或 **Vue 3 Composition API 版本的 useProgress()**，欢迎继续提问！
